PORT=8787

# AWS credentials are resolved through the normal SDK provider chain.
# You can export AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_SESSION_TOKEN,
# use `aws configure`, SSO, or another supported credential source.

# Optional defaults. The iOS app can also send these values in the request body.
AMAZON_CONNECT_INSTANCE_ID=
AMAZON_CONNECT_CONTACT_FLOW_ID=
AMAZON_CONNECT_REGION=us-east-1
AMAZON_CONNECT_SOURCE=ios-floating-chat-poc
